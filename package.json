{
  "name": "akademikai-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "backend": "cd python && PYTHONPATH=. python -m uvicorn backend.main:app --reload --port 8000",
    "db:push": "cd frontend && npm run db:push",
    "db:generate": "cd frontend && npm run db:generate",
    "db:studio": "cd frontend && npm run db:studio",
    "lint": "cd frontend && npm run lint",
    "install:all": "cd frontend && npm install && cd ../python && pip install -r requirements.txt && pip install -r backend/requirements.txt"
  }
}
